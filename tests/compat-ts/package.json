{
  "name": "axond-compat-ts",
  "version": "0.0.0",
  "private": true,
  "description": "TypeScript provider-SDK compatibility lane: the vendors' own Node SDKs against a real axond and a local fake upstream.",
  "type": "module",
  "license": "MIT OR Apache-2.0",
  "engines": {
    "node": ">=22.12.0 <23"
  },
  "scripts": {
    "build": "node scripts/typecheck.mjs",
    "test": "npm run build && node --test 'build/*.test.js'"
  },
  "dependencies": {
    "@anthropic-ai/sdk": "0.115.0",
    "openai": "7.4.0"
  },
  "devDependencies": {
    "@types/node": "22.20.1",
    "typescript": "7.0.2"
  }
}

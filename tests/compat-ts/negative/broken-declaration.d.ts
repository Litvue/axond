/**
 * A declaration file that does not type-check, standing in for a future SDK
 * release whose shipped `.d.ts` is broken or references a peer that is not
 * installed. `skipLibCheck: false` is what makes this an error the lane reports
 * rather than something it quietly trusts.
 */

export declare const brokenUpstream: ThisTypeIsNotDeclaredAnywhere;

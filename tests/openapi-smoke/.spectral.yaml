extends:
  - spectral:oas
rules:
  info-contact: off
  info-license: off
  operation-tags: off
  # utoipa puts rustdoc on `summary`; requiring a separate `description` is noise.
  operation-description: off

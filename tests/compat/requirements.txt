# Pinned exactly: the point of this lane is that a provider SDK release is what
# changed, not the pin. Versions are chosen at least a week after publication.
openai==2.50.0
anthropic==0.120.0
pytest==9.1.1

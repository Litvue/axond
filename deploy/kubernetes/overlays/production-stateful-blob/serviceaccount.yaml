# The object-store adapter uses workload identity rather than a Kubernetes
# Secret. Bind this dedicated ServiceAccount to the cloud identity allowed to
# read and publish only the configured environment container.
apiVersion: v1
kind: ServiceAccount
metadata:
  name: axond-blob
  namespace: axond

apiVersion: v1
kind: Service
metadata:
  name: axond
  namespace: axond
spec:
  type: ClusterIP
  selector:
    app.kubernetes.io/name: axond
  ports:
    - name: http
      port: 8080
      targetPort: http

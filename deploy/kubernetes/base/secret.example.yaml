# Public placeholders for manifest validation and local evaluation only.
# Replace both values or supply an equivalent Secret before exposing Axond.
apiVersion: v1
kind: Secret
metadata:
  name: axond-secrets
  namespace: axond
type: Opaque
stringData:
  GW_PLATFORM_OPENAI_API_KEY: placeholder-openai-key
  GW_INBOUND_PLATFORM_KEY: replace-before-exposing
